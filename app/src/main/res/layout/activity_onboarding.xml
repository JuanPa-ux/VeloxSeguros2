<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_onboarding.xml
    Onboarding minimalista:
    - Botón "Saltar" arriba a la derecha
    - ViewPager2 ocupa todo el espacio central
    - Indicador de puntos (dots) encima del botón
    - Botón "Siguiente" / "Empezar" al fondo
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackground">

    <!-- Saltar — esquina superior derecha -->
    <TextView
        android:id="@+id/tvSaltar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Saltar"
        android:textColor="@color/colorTextSecondary"
        android:textSize="13sp"
        android:letterSpacing="0.05"
        android:padding="20dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ViewPager2 — ocupa el espacio central -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/tvSaltar"
        app:layout_constraintBottom_toTopOf="@id/dotsContainer"/>

    <!-- Indicador de puntos -->
    <LinearLayout
        android:id="@+id/dotsContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="24dp"
        app:layout_constraintBottom_toTopOf="@id/btnSiguiente"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Los dots se generan dinámicamente en OnboardingActivity -->
        <View
            android:id="@+id/dot0"
            android:layout_width="6dp"
            android:layout_height="6dp"
            android:layout_marginHorizontal="4dp"
            android:background="@color/colorPrimary"/>

        <View
            android:id="@+id/dot1"
            android:layout_width="6dp"
            android:layout_height="6dp"
            android:layout_marginHorizontal="4dp"
            android:background="@color/colorBorder"/>

        <View
            android:id="@+id/dot2"
            android:layout_width="6dp"
            android:layout_height="6dp"
            android:layout_marginHorizontal="4dp"
            android:background="@color/colorBorder"/>

    </LinearLayout>

    <!-- Botón principal -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSiguiente"
        style="@style/Widget.Velox.Button"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="24dp"
        android:layout_marginBottom="40dp"
        android:text="Siguiente"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
